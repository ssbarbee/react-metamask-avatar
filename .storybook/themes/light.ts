import { create } from '@storybook/theming';

export default create({
  base: 'light',
  appBorderRadius: 4,
  inputBorderRadius: 4,
  brandTitle: 'react-metamask-avatar',
  brandTarget: '_self',
});
